{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability",
  "windows": ["control", "display"],
  "permissions": [
    "core:default",
    "shell:allow-open",
    "deep-link:default",
    "dialog:default"
  ]
}
